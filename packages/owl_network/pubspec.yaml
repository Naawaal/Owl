name: owl_network
description: Network client and SSE stream reader for Owl MOBA HUD.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  owl_core:
    path: ../owl_core

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
